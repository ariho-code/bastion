# Public site URL — used for canonical links, sitemap, Open Graph and JSON-LD.
# Set this in Vercel → Project → Settings → Environment Variables to your
# production domain (e.g. https://bastionscan.com). Falls back to the Vercel
# preview URL if unset.
NEXT_PUBLIC_SITE_URL=https://bastion-arihos-projects.vercel.app
